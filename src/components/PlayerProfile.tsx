import React, { useState, useEffect } from 'react';

export interface IPlayerProfileProps {
    summonerName: string;
}

const PlayerProfile = (props: IPlayerProfileProps) : JSX.Element => {
    return (
        <div className='playerProfile'>

        </div>
    );
}

export default PlayerProfile;